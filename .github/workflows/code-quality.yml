name: Code Quality Analysis for Backend Module

on:
  workflow_call:

jobs:
  code-quality-associados:
    name: Lint do módulo ms-associados
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Run Checkstyle
        run: mvn -B -f ms-associados/pom.xml spotless:apply

      - name: Compile Backend Module
        run: mvn -B -f ms-associados/pom.xml clean package -DskipTests

      - name: Run Checkstyle
        run: mvn -B -f ms-associados/pom.xml checkstyle:check

      - name: Generate Checkstyle report
        run: mvn -B -f ms-associados/pom.xml checkstyle:checkstyle

      - name: Run PMD check
        run: mvn -B -f ms-associados/pom.xml pmd:check

      - name: generate PMD report
        run: mvn -B -f ms-associados/pom.xml pmd:pmd
      
      - name: Run spotbugs check
        run: mvn -B -f ms-associados/pom.xml spotbugs:check

      - name: Generate SpotBugs report
        run: mvn -B -f ms-associados/pom.xml spotbugs:spotbugs
        
  code-quality-pautas:
    name: Lint do módulo ms-pautas
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Run Checkstyle
        run: mvn -B -f ms-pautas/pom.xml spotless:apply

      - name: Compile Backend Module
        run: mvn -B -f ms-pautas/pom.xml clean package -DskipTests

      - name: Run Checkstyle
        run: mvn -B -f ms-pautas/pom.xml checkstyle:check

      - name: Generate Checkstyle report
        run: mvn -B -f ms-pautas/pom.xml checkstyle:checkstyle

      - name: Run PMD check
        run: mvn -B -f ms-pautas/pom.xml pmd:check

      - name: generate PMD report
        run: mvn -B -f ms-pautas/pom.xml pmd:pmd
      
      - name: Run spotbugs check
        run: mvn -B -f ms-pautas/pom.xml spotbugs:check

      - name: Generate SpotBugs report
        run: mvn -B -f ms-pautas/pom.xml spotbugs:spotbugs
  code-quality-sessoes:
    name: Lint do módulo ms-sessoes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Run Checkstyle
        run: mvn -B -f ms-sessoes/pom.xml spotless:apply

      - name: Compile Backend Module
        run: mvn -B -f ms-sessoes/pom.xml clean package -DskipTests

      - name: Run Checkstyle
        run: mvn -B -f ms-sessoes/pom.xml checkstyle:check

      - name: Generate Checkstyle report
        run: mvn -B -f ms-sessoes/pom.xml checkstyle:checkstyle

      - name: Run PMD check
        run: mvn -B -f ms-sessoes/pom.xml pmd:check

      - name: generate PMD report
        run: mvn -B -f ms-sessoes/pom.xml pmd:pmd
      
      - name: Run spotbugs check
        run: mvn -B -f ms-sessoes/pom.xml spotbugs:check

      - name: Generate SpotBugs report
        run: mvn -B -f ms-sessoes/pom.xml spotbugs:spotbugs
  code-quality-api-gateway:
    name: Lint do módulo api-gateway
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Run Checkstyle
        run: mvn -B -f api-gateway/pom.xml spotless:apply

      - name: Compile Backend Module
        run: mvn -B -f api-gateway/pom.xml clean package -DskipTests

      - name: Run Checkstyle
        run: mvn -B -f api-gateway/pom.xml checkstyle:check

      - name: Generate Checkstyle report
        run: mvn -B -f api-gateway/pom.xml checkstyle:checkstyle

      - name: Run PMD check
        run: mvn -B -f api-gateway/pom.xml pmd:check

      - name: generate PMD report
        run: mvn -B -f api-gateway/pom.xml pmd:pmd
      
      - name: Run spotbugs check
        run: mvn -B -f api-gateway/pom.xml spotbugs:check

      - name: Generate SpotBugs report
        run: mvn -B -f api-gateway/pom.xml spotbugs:spotbugs

  code-quality-service-discovery:
    name: Lint do módulo service-discovery
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Run Checkstyle
        run: mvn -B -f service-discovery/pom.xml spotless:apply

      - name: Compile Backend Module
        run: mvn -B -f service-discovery/pom.xml clean package -DskipTests

      - name: Run Checkstyle
        run: mvn -B -f service-discovery/pom.xml checkstyle:check

      - name: Generate Checkstyle report
        run: mvn -B -f service-discovery/pom.xml checkstyle:checkstyle

      - name: Run PMD check
        run: mvn -B -f service-discovery/pom.xml pmd:check

      - name: generate PMD report
        run: mvn -B -f service-discovery/pom.xml pmd:pmd
      
      - name: Run spotbugs check
        run: mvn -B -f service-discovery/pom.xml spotbugs:check

      - name: Generate SpotBugs report
        run: mvn -B -f service-discovery/pom.xml spotbugs:spotbugs