name: CI Pipeline for ms-associados Module

on:
  push:
    branches:
      - main

jobs:
  code-quality-associados:
    name: Lint do módulo ms-associados
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Run Checkstyle
        run: mvn -B -f ms-associados/pom.xml spotless:apply

      - name: Compile Backend Module
        run: mvn -B -f ms-associados/pom.xml clean package -DskipTests

      - name: Run Checkstyle
        run: mvn -B -f ms-associados/pom.xml checkstyle:check

      - name: Generate Checkstyle report
        run: mvn -B -f ms-associados/pom.xml checkstyle:checkstyle

      - name: Run PMD check
        run: mvn -B -f ms-associados/pom.xml pmd:check

      - name: generate PMD report
        run: mvn -B -f ms-associados/pom.xml pmd:pmd
      
      #- name: Run spotbugs check
      #  run: mvn -B -f ms-associados/pom.xml spotbugs:check

      #- name: Generate SpotBugs report
      #  run: mvn -B -f ms-associados/pom.xml spotbugs:spotbugs
  
  test-associados:
    needs: [code-quality-associados]
    name: Execute Unit tests for Backend Module
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
  
      - name: Roda os testes do módulo ms-associados
        run: mvn -B -f ms-associados/pom.xml clean package
      - name: Run Code Quality Analysis do módulo ms-associados
        run: mvn -B -f ms-associados/pom.xml jacoco:report
      - name: Upload Coverage Report do módulo ms-associados
        uses: actions/upload-artifact@v6
        with:
          name: coverage-report
          path: ms-associados/target/site/jacoco/
          retention-days: 1

  build-associados:
    needs: [test-associados]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      - name: Build ms-associados module
        run: mvn -B -f ms-associados/pom.xml clean package -DskipTests
      
      - name: Upload Backend Artifact
        uses: actions/upload-artifact@v6
        with:
          name: ms-associados-jar
          path: ms-associados/target/*.jar
          retention-days: 1

#zap-scan:
  #  needs: build-project
  #  uses: ./.github/workflows/zapscan.yml
  #  secrets: inherit
  
  #docker:
  #  needs: zap-scan
  #  uses: ./.github/workflows/docker.yml
  #  secrets: inherit
  
  #deploy_google:
  #  needs: docker
  #  uses: ./.github/workflows/release-gcloud.yml
  #  secrets: inherit